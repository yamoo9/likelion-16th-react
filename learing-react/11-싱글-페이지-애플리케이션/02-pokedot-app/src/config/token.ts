/**
 * [인증 토큰 저장 키: TOKEN]
 * 로컬 스토리지(localStorage)나 쿠키에 JWT 토큰을 저장할 때 사용하는 식별자입니다.
 * 
 * @example
 * localStorage.setItem(TOKEN, '<jwt-token>')
 * const savedToken = localStorage.getItem(TOKEN)
 */
export const TOKEN = '@pokedot/refreshToken'