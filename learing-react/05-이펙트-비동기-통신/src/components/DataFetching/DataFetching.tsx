import styles from './DataFetching.module.css'

export default function PostList() {
  
  return (
    <article className={styles.container}>
      <h2 className={styles.title}>최신 포스트 리스트</h2>

      {/* 1. 로딩 상태 UI */}

      {/* 2. 에러 상태 UI */}

      {/* 3. 성공 상태 UI */}
      
    </article>
  )
}
